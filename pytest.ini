[pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    slow: marks tests as slow (requires NEPER/CUBIT/MOOSE)
    requires_neper: marks tests that need NEPER
    requires_cubit: marks tests that need CUBIT/SCULPT
    requires_moose: marks tests that need MOOSE/PUMA
